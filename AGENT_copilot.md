# AGENT_copilot.md — Конвейер п. 23 для GitHub Copilot (Codespaces), ТРЁХФАЗНЫЙ режим

**Назначение:** пошаговая инструкция для анализа практики по п. 23 Постановления Пленума ВС РФ от 29.05.2018 № 15 в среде **GitHub Copilot (agent mode) в Codespaces**, где **нет параллельных субагентов** (в отличие от Claude Code). Адаптация исходного [п.23/AGENT.md](п.23/AGENT.md).

---

## ⛔ СТРОГИЙ SCOPE — читать первым

Обрабатываются **ТОЛЬКО** три суда:

| Суд | Файлы | Тип | Определений | Групп |
|---|---|---|---|---|
| **7кас** | `7кас_ч.1..4.txt` | кассация | 183 | 20 |
| **8кас** | `8кас_ч.1..4.txt` | кассация | 116 | 13 |
| **СОЮ Москвы и области** | `СОЮ_Москвы_1..3.txt` | **апелляция Мосгорсуда** | 120 | 13 |

**НЕ размечать и НЕ анализировать:** 1–5кас, 9кас (уже готовы), 6кас (дела-файлы уже есть), СОЮ Санкт_Ленинград и Свердловской (пустые). Готовые `итоговые_выводы.md` судов 1–5, 9кас используются **только для сравнения** на шаге «III. Сравнение».

Этот файл подключается автоматически через `.github/copilot-instructions.md` — пользователю открывать его не нужно.

---

## 🔀 ТРИ ФАЗЫ (скрипт → Luna 5.6 → Kimi K3)

Конвейер разделён на три фазы под разные «исполнители», чтобы **минимизировать расход кредитов Max**:

| Фаза | Кто | Что делает | Объём чтения | Стоимость |
|---|---|---|---|---|
| **0. Разметка** | скрипт `markup.py` | границы, группы, заготовки | — | **бесплатно** |
| **1. Черновой разбор** | **Luna 5.6** (дёшево) | читает сырой текст, заполняет `дела_XX-YY.md` + строки таблиц | весь текст (~МБ) | дёшево |
| **2. Синтез** | **Kimi K3** (дорого) | проверяет черновики, пишет `итоговые_выводы.md`, сверяет таблицы, сравнение судов | только черновики | дорого, но мало |

**Зачем так:** сырой текст суда — 2–3 МБ. Если его будет читать дорогой Kimi K3, кредиты уйдут на чтение, а не на мышление. Дешёвая Luna 5.6 снимает с текста структурированные черновики, а Kimi K3 работает уже с ними — дорогие токены тратятся только на синтез.

### Команды пользователя

| Команда | Активная модель | Что запускает |
|---|---|---|
| `черновик 7кас` (или 8кас / СОЮ) | **Luna 5.6** | ФАЗА 1 — черновой разбор всех групп суда |
| `синтез 7кас` (или 8кас / СОЮ) | **Kimi K3** | ФАЗА 2 — итоговые выводы, таблицы, сравнение |
| `продолжи 7кас` | текущая | возобновление после обрыва (см. ниже) |

### 🔔 Сигналы переключения модели

- После `python markup.py` → **«ВКЛЮЧАЙ LUNA 5.6»** → пользователь переключает модель и пишет `черновик 7кас`.
- Когда Luna заполнила **все** группы суда → агент выводит **«ВКЛЮЧАЙ KIMI K3 — черновики готовы»** → пользователь переключает модель и пишет `синтез 7кас`.

### ▶️ Возобновление работы (новая беседа / обрыв контекста)

Контекст одной беседы ограничен — суд из 13–20 групп может не влезть целиком. Это штатно. Если пользователь пишет **«продолжи 7кас»**:

1. Открой `п.23/<суд>/разметка.md` — там полный список групп.
2. Определи, какие `дела_XX-YY.md` уже **заполнены** (содержат блоки `### Дело N`), а какие — ещё **заготовки** (только HTML-комментарий `<!-- ЧЕРНОВИК ... -->`).
3. Продолжи с **первой незаполненной группы**. Уже заполненные НЕ переделывай.
4. Если все группы заполнены, но нет `итоговые_выводы.md` — значит, пора в ФАЗУ 2 (нужна Kimi K3): сообщи пользователю «черновики готовы, включи Kimi K3 и напиши "синтез 7кас"».

---

## 0. Контекст задачи

- Источник: `п.23/<суд>/<часть>.txt`. Для 7кас/8кас — 4 части, для СОЮ — 3 части.
- Предмет анализа — **только применение п. 23 Пленума ВС РФ № 15**: при отсутствии письменных доказательств размера зарплаты суд вправе определить её из обычного вознаграждения работника его квалификации в данной местности, при невозможности — из МЗП в субъекте РФ (ст. 133.1 ТК).
- **СОЮ Москвы — апелляция** (Мосгорсуд), не кассация: предмет тот же (п. 23), но учитывай, что это вторая инстанция.
- Текущий статус — в [п.23/СТАТУС.md](п.23/СТАТУС.md). **Обновляй после каждого суда.**

---

## ФАЗА 0. Разметка — скрипт `markup.py` (без модели)

Готовый результат — `п.23/<суд>/разметка.md` (группа | часть | строки A–B | число дел) + отметки о дублях + заготовки файлов. **Вручную не размечай, если скрипт уже отработал.**

Известные дубли (из разметки):
- **СОЮ Москвы:** дело **41** — дубль дела **40** (определение 33-2025/2025 на стыке ч.1→ч.2). Нумерацию не сдвигать, дело 41 пометить дублём.

---

## ФАЗА 1. Черновой разбор — Luna 5.6 (команда `черновик Nкас`)

**Задача:** прочитать сырой текст и заполнить черновики. Работай **последовательно по группам** из `разметка.md`. Для КАЖДОЙ группы:

1. Прочитай диапазон строк (Read с offset/limit, несколько вызовов). **ВНИМАНИЕ:** Read обрезает строки длиннее ~2000 символов — дочитывай через терминал: `sed -n 'NNp' "<путь>"`.
2. Заполни заготовку `п.23/<суд>/дела_<диапазон>.md` по структуре ниже.
3. **Сразу дописывай** строки в `сводная_таблица.md` и `сводная_таблица.csv` (форматы — в ФАЗЕ 2), чтобы не держать всё в контексте.
4. Переходи к следующей группе.

**Структура черновика `дела_<диапазон>.md`:**

```
# <Название суда> — анализ определений, дела <диапазон>
**Источник:** <файл>, строки A–B

Для КАЖДОГО определения:
### Дело N <номер дела> (определение от <дата> N <номер>)
1. **Фабула** (1–2 предложения: истец/должность, ответчик — физлицо/ИП/организация,
   обсуждался ли статус СМП/микропредприятия, как оформлялись отношения, заявленные суммы).
2. **Применение п. 23**: цитирует ли (да/нет + короткая цитата).
3. **Как определён размер зарплаты**: (а) обычное вознаграждение — ЧЕМ подтверждено;
   (б) МЗП/МРОТ; (в) иной способ. Конкретные суммы.
4. **Бремя доказывания и оценка доказательств**.
5. **Итог** (оставлено/отменено, в какой части).
6. **Ключевой вывод по п. 23** (1–2 предложения).

В конце файла — "## Общие наблюдения по группе": 4–6 пунктов.
```

**Когда все группы суда заполнены** — выведи маркер: **«ВКЛЮЧАЙ KIMI K3 — черновики готовы, напиши "синтез Nкас"»**.

---

## ФАЗА 2. Синтез — Kimi K3 (команда `синтез Nкас`)

**Задача:** из черновиков сделать итоговый продукт. Ты работаешь с **уже заполненными** `дела_XX-YY.md`, а не с сырым текстом.

### Шаг 1. Контроль и верификация
- Проверь: все `дела_XX-YY.md` заполнены (не заготовки), размеры адекватны (25–50 КБ).
- **Выборочно сверь** 2–3 дела из разных групп с оригиналом (через `sed -n 'NNp'`) — убедись, что черновик Luna точен (суммы, способ, итог). Если находишь системные ошибки — сообщи пользователю и отметь проблемные группы.
- Дубликаты дел — в таблицу и нумерацию с пометкой «дубль».

### Шаг 2. Итоговые выводы
Создай `п.23/<суд>/итоговые_выводы.md`:
1. Шапка: источник, число определений/уникальных дел, связанные и повторные дела, ссылки на групповые файлы и таблицы.
2. «I. Выводы по каждому делу» — по 2–4 строки на дело.
3. «II. Общие выводы по практике <суда>» — 8–11 пунктов по стандартным рубрикам (раздел «Рубрики» ниже).
4. «III. Сравнение с уже обработанными судами» — 3–6 пунктов (читай `итоговые_выводы.md` судов 1–5, 9кас).

### Шаг 3. Таблицы и статус
1. `сводная_таблица.csv` — BOM уже в заготовке; разделитель «;», все поля в двойных кавычках. Колонки: №; Определение; Дата; Дело N; Истец (должность); Ответчик; Способ определения размера ЗП; Размер / база расчёта; Итог; Ключевой вывод по п. 23. **Сверь** заполненные Luna строки, поправь при ошибках.
2. `сводная_таблица.md` — те же строки, 6 колонок: № | Определение, дата | Стороны | Способ | Итог | Ключевой вывод.
3. Обнови `п.23/СТАТУС.md` (статус суда → ✅, дата, число дел, ссылки).

---

## Рубрики анализа (калибровка по 1кас/2кас)

1. **Сфера п. 23**: формула «актуальны для всех субъектов»; проверка статуса СМП/микропредприятия.
2. **Иерархия способов**: письменные доказательства → обычное вознаграждение → МЗП.
3. **Обычное вознаграждение**: источники (Росстат, вакансии, зарплата коллег); сопоставимость статистики.
4. **МРОТ/МЗП**: условия перехода; отмены за механический МРОТ; география.
5. **Бремя доказывания**: на работодателе/работнике; «односторонние» документы.
6. **Свидетели и вакансии**: допустимость (у судов расходится).
7. **Техника взыскания**: прямой расчёт из статистики vs «заявленное + контроль соразмерности».
8. **Связки с другими нормами**: ст. 236, 139, 184, 279 ТК, ФЗ-125, преюдиция.
9. **Типовые ошибки нижестоящих судов → основания отмен** (с номерами дел).
10. **Статистика исходов**: оставлено/отменено, доля отмен по существу п. 23.

## Технические правила (грабли)

- **Read обрезает строки >2000 символов** → дочитывай через `sed -n 'NNp'`.
- **Кириллические пути** — в терминале заключай в кавычки; возможен неразрывный пробел в именах.
- **Дубликаты дел** — помечай, нумерацию не сдвигай (СОЮ: дело 41 = дубль 40).
- **Обезличенные тексты** — номера могут отсутствовать («ДД.ММ.ГГГГ», «ФИО1») — фиксируй «обезличено», не выдумывай.
- **Многочастные файлы** — сквозная нумерация, стыки уже проверены скриптом (см. разметка.md).
- **Окончания строк** — в репозитории LF (настроено через .gitattributes); не меняй.

## После всех трёх судов

Дополни `п.23/сравнение_всех_кас.md` (или создай, если нет) блоками по 7кас, 8кас, СОЮ: матрица по рубрикам, рейтинг «про-работническости», расхождения для унификации, эталонные определения.
