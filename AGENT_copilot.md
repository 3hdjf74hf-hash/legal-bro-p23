# AGENT_copilot.md — Конвейер п. 23 для GitHub Copilot (Codespaces), двухфазный режим

**Назначение:** пошаговая инструкция для анализа кассационной/апелляционной практики по п. 23 Постановления Пленума ВС РФ от 29.05.2018 № 15 в среде **GitHub Copilot (agent mode) в Codespaces**, где **нет параллельных субагентов** (в отличие от Claude Code). Адаптация исходного [п.23/AGENT.md](п.23/AGENT.md).

---

## ⛔ СТРОГИЙ SCOPE — читать первым

Обрабатываются **ТОЛЬКО** три суда:

| Суд | Файлы | Тип | Определений | Групп |
|---|---|---|---|---|
| **7кас** | `7кас_ч.1..4.txt` | кассация | 183 | 20 |
| **8кас** | `8кас_ч.1..4.txt` | кассация | 116 | 13 |
| **СОЮ Москвы и области** | `СОЮ_Москвы_1..3.txt` | **апелляция Мосгорсуда** | 120 | 13 |

**НЕ размечать и НЕ анализировать:** 1–5кас, 9кас (уже готовы), 6кас (дела-файлы уже есть), СОЮ Санкт_Ленинград и Свердловской (пустые). Готовые `итоговые_выводы.md` судов 1–5, 9кас используются **только для сравнения** на шаге «III. Сравнение».

**Запуск:** пользователь пишет «п23: 7кас» / «п23: 8кас» / «п23: СОЮ» или «выполни AGENT_copilot.md для 7кас». Этот файл подключается автоматически через `.github/copilot-instructions.md` — пользователю открывать его не нужно.

### ▶️ Возобновление работы (новая беседа / обрыв контекста)

Контекст одной беседы ограничен — суд из 13–20 групп может не влезть целиком. Это штатно. Если пользователь пишет **«продолжи 7кас»** (или 8кас / СОЮ) в новой беседе:

1. Открой `п.23/<суд>/разметка.md` — там полный список групп.
2. Определи, какие `дела_XX-YY.md` уже **заполнены** (содержат блоки `### Дело N`), а какие — ещё **заготовки** (только HTML-комментарий `<!-- ЗАПОЛНИТЬ ... -->`).
3. Продолжи с **первой незаполненной группы**, далее по разметке. Уже заполненные группы НЕ переделывай.
4. Если `итоговые_выводы.md` и таблицы ещё не созданы — создай их после последней группы (шаги 4–5).

---

## 🔀 ДВУХФАЗНЫЙ РЕЖИМ (Luna → Kimi K3)

Конвейер разделён на две фазы под разные модели, чтобы экономить кредиты Max:

### ФАЗА 1 — техническая (слабая модель: Luna 5.6 / любая)

Только механика, без юридического анализа. **Предпочтительно — скриптом:**

```bash
python markup.py 7кас      # или 8кас / "СОЮ Москвы и области" / без аргумента — все три
```

Скрипт сам: найдёт границы определений, проверит стыки на дубли, разобьёт на группы по 10, создаст `разметка.md` и заготовки (`дела_XX-YY.md`, `сводная_таблица.csv`, `сводная_таблица.md`).

**Если скрипт недоступен** — фазу 1 делает слабая модель вручную: Grep по заголовку → группы по 10 → строковые диапазоны → заготовки.

### 🔔 СИГНАЛ ПЕРЕКЛЮЧЕНИЯ

Когда фаза 1 завершена (разметка и заготовки готовы), в чат выводится маркер:

> **МУЖИК, ВКЛЮЧАЙ KIMI K3 — разметка готова, можно запускать анализ групп.**

Пользователь вручную переключает модель в Copilot на **Kimi K3** и пишет: «продолжи по разметке».

### ФАЗА 2 — аналитическая (Kimi K3, высокий effort)

Читает `разметка.md` и **последовательно** обрабатывает группы (шаг 2 ниже), затем итоги и таблицы (шаги 3–5).

---

## 0. Контекст задачи

- Источник: `п.23/<суд>/<часть>.txt`. Для 7кас/8кас — 4 части, для СОЮ — 3 части.
- Предмет анализа — **только применение п. 23 Пленума ВС РФ № 15**: при отсутствии письменных доказательств размера зарплаты суд вправе определить её из обычного вознаграждения работника его квалификации в данной местности, при невозможности — из МЗП в субъекте РФ (ст. 133.1 ТК).
- **СОЮ Москвы — апелляция** (Мосгорсуд), не кассация: предмет тот же (п. 23), но учитывай, что это вторая инстанция — в выводах отмечай, где апелляция правит после кассации.
- Текущий статус — в [п.23/СТАТУС.md](п.23/СТАТУС.md). **Обновляй после каждого суда.**

## 1. Конвейер

### Шаг 1. Разметка — ФАЗА 1 (скрипт `markup.py`)

Готовый результат — `п.23/<суд>/разметка.md` со списком групп (группа | часть | строки A–B | число дел) и отметками о дублях на стыках. **Вручную не размечай, если скрипт уже отработал.**

Известные дубли (из разметки):
- **СОЮ Москвы:** дело **41** — дубль дела **40** (определение 33-2025/2025 на стыке ч.1→ч.2). Нумерацию не сдвигать, дело 41 пометить дублём.

### Шаг 2. Последовательная обработка групп — ФАЗА 2 (Kimi K3)

**В Copilot нет субагентов** — обрабатывай группы **по одной, последовательно**. Для КАЖДОЙ группы из `разметка.md`:

1. Прочитай диапазон строк (Read с offset/limit, несколько вызовов). **ВНИМАНИЕ:** Read обрезает строки длиннее ~2000 символов — дочитывай через терминал: `sed -n 'NNp' "<путь>"`.
2. Заполни заготовку `п.23/<суд>/дела_<диапазон>.md` по структуре ниже.
3. Переходи к следующей группе.

**Промпт-структура для каждой группы** (файл `дела_<диапазон>.md`):

```
# <Название суда> — анализ определений, дела <диапазон>
**Источник:** <файл>, строки A–B

Для КАЖДОГО определения:
### Дело N <номер дела> (определение от <дата> N <номер>)
1. **Фабула** (1–2 предложения: истец/должность, ответчик — физлицо/ИП/организация,
   обсуждался ли статус СМП/микропредприятия, как оформлялись отношения, заявленные суммы).
2. **Применение п. 23**: цитирует ли (да/нет + короткая цитата).
3. **Как определён размер зарплаты**: (а) обычное вознаграждение — ЧЕМ подтверждено;
   (б) МЗП/МРОТ; (в) иной способ. Конкретные суммы.
4. **Бремя доказывания и оценка доказательств**.
5. **Итог** (оставлено/отменено, в какой части).
6. **Ключевой вывод по п. 23** (1–2 предложения).

В конце файла — "## Общие наблюдения по группе": 4–6 пунктов.
```

**После каждой группы** дописывай строки в `сводная_таблица.md` и `сводная_таблица.csv` (форматы — в шаге 5), чтобы не держать всё в контексте.

### Шаг 3. Контроль

- После всех групп проверь: все `дела_XX-YY.md` заполнены (не заготовки), размеры адекватны (25–50 КБ).
- Дубликаты дел — в таблицу и нумерацию включай с пометкой «дубль».

### Шаг 4. Итоговые выводы — ФАЗА 2 (Kimi K3)

Создай `п.23/<суд>/итоговые_выводы.md`:
1. Шапка: источник, число определений/уникальных дел, связанные и повторные дела, ссылки на групповые файлы и таблицы.
2. «I. Выводы по каждому делу» — по 2–4 строки на дело.
3. «II. Общие выводы по практике <суда>» — 8–11 пунктов по стандартным рубрикам (раздел 2 ниже).
4. «III. Сравнение с уже обработанными судами» — 3–6 пунктов (читай `итоговые_выводы.md` судов 1–5, 9кас).

### Шаг 5. Таблицы и статус — ФАЗА 2

1. `сводная_таблица.csv` — BOM уже в заготовке; разделитель «;», все поля в двойных кавычках. Колонки: №; Определение; Дата; Дело N; Истец (должность); Ответчик; Способ определения размера ЗП; Размер / база расчёта; Итог; Ключевой вывод по п. 23.
2. `сводная_таблица.md` — те же строки, 6 колонок: № | Определение, дата | Стороны | Способ | Итог | Ключевой вывод.
3. Обнови `п.23/СТАТУС.md` (статус суда → ✅, дата, число дел, ссылки).

## 2. Стандартные рубрики анализа (калибровка по 1кас/2кас)

1. **Сфера п. 23**: формула «актуальны для всех субъектов»; проверка статуса СМП/микропредприятия.
2. **Иерархия способов**: письменные доказательства → обычное вознаграждение → МЗП.
3. **Обычное вознаграждение**: источники (Росстат, вакансии, зарплата коллег); сопоставимость статистики.
4. **МРОТ/МЗП**: условия перехода; отмены за механический МРОТ; география.
5. **Бремя доказывания**: на работодателе/работнике; «односторонние» документы.
6. **Свидетели и вакансии**: допустимость (у судов расходится).
7. **Техника взыскания**: прямой расчёт из статистики vs «заявленное + контроль соразмерности».
8. **Связки с другими нормами**: ст. 236, 139, 184, 279 ТК, ФЗ-125, преюдиция.
9. **Типовые ошибки нижестоящих судов → основания отмен** (с номерами дел).
10. **Статистика исходов**: оставлено/отменено, доля отмен по существу п. 23.

## 3. Технические правила (грабли)

- **Read обрезает строки >2000 символов** → дочитывай через `sed -n 'NNp'`.
- **Кириллические пути** — в терминале заключай в кавычки; возможен неразрывный пробел в именах.
- **Дубликаты дел** — помечай, нумерацию не сдвигай (СОЮ: дело 41 = дубль 40).
- **Обезличенные тексты** — номера могут отсутствовать («ДД.ММ.ГГГГ», «ФИО1») — фиксируй «обезличено», не выдумывай.
- **Многочастные файлы** — сквозная нумерация, стыки уже проверены скриптом (см. разметка.md).
- **Окончания строк** — в репозитории LF (настроено через .gitattributes); не меняй.

## 4. После всех трёх судов

Дополни `п.23/сравнение_всех_кас.md` (или создай, если нет) блоками по 7кас, 8кас, СОЮ: матрица по рубрикам, рейтинг «про-работническости», расхождения для унификации, эталонные определения.
