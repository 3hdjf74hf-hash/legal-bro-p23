# Фиксируем LF для всех текстовых файлов — критично для нумерации строк
# (sed -n 'NNp', Grep -n) в Linux-окружении Codespaces.
* text=auto eol=lf

*.txt text eol=lf
*.md  text eol=lf
*.csv text eol=lf
*.py  text eol=lf
*.json text eol=lf
